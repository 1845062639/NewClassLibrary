# WORKLOG 2026-03-19

## 路线调整
用户明确要求：主方向不是继续修补旧 App 框架，而是在旧系统基础上设计并新建一套新的软件项目。

## 已执行
- 创建 `next-gen/` 工作区
- 建立新项目分层目录：
  - `StandardTestNext.App`
  - `StandardTestNext.Test`
  - `StandardTestNext.Contracts`
- 输出新架构草案与迁移计划
- 在新 Contracts 中先定义首批 topic 与 DTO
- 补齐 `StandardTestNext.sln`
- 补齐 App/Test/Contracts 的可运行项目骨架
- 落第一条垂直切片：
  - Test 侧构造 `MotorRatedParamsContract`
  - App 侧输出 `MotorRealtimeSampleContract`

## 当前结论
- 旧系统继续作为参考与迁移素材
- 新系统才是后续主干
- 旧 App 的局部示范改造只保留其“验证新边界可行”的价值，不再作为最终主路径

## 本小时新增落地
- 在 `StandardTestNext.Contracts` 中补齐 `TestCommandContract` 与 `DeviceStatusContract`
- 将 `ContractTopics` 明确为版本化 topic：`stnext/.../v1`
- 在 App 侧补 `IMessageSubscriber`、增强 `InMemoryMessageBus` 为可订阅发布的测试总线
- 在 App 侧补 `DeviceStatusReportingService` 与 `AppCommandConsumer`
- 在 Test 侧补 `TestCommandBuilder` 与 `TestRuntimeOrchestrator`
- 将 App/Test 两个启动入口都串成同一条 demo 链路：App 先报 Ready 与采样，Test 再下发启动命令，App 消费命令后回报运行状态
- 在 `StandardTestNext.Test` 新增记录主线骨架：`ProductDefinition`、`TestRecordAggregate`、`TestRecordItemAggregate`、`RecordAttachment`
- 新增 `TestRecordAggregateBuilder`，明确 phase-1 仍保留 `RatedParams/Data` 为 JSON 载荷，避免过早把旧系统强耦合模型直接搬进新主干
- 更新 `TestBootstrap`，让 demo 链路在命令发布后继续生成一份试验记录聚合摘要，作为 Test 侧记录主线的最小闭环
- 继续把待办往前推：补 `Application/Abstractions` 下三类仓储接口（记录 / 产品 / 附件）
- 新增 `TestRecordItemMapper`，先把实时样本拆成“关键点记录”和“连续采样记录”两类分项，避免后续记录聚合逻辑继续堆在一个 Builder 里
- 新增 `Infrastructure/Persistence` 下内存仓储样板，并把 `TestBootstrap` 串到“聚合生成 -> 仓储保存样板”的最小闭环，作为后续接数据库/SQLite 的落点
- 将 Test 文档里的下一步待办继续前推：新增 `ITestReportRenderer`、`JsonTestReportRenderer`、`TestReportExportService`，先把“记录聚合 -> 报告输出抽象”的边界落成最小可替换接口，phase-1 先输出 JSON 报告预览，后续再接正式模板/导出链路
- 同步增强 `TestRecordConsolePresenter` 与 `TestBootstrap`，让 demo 闭环在保存聚合后立刻走一次报告渲染预览，避免“报告输出”继续停留在纸面待办
- 继续把报告抽象补成最小可替换闭环：`TestReportExportService` 改为返回 `Format + Content`，避免调用方再手写格式字符串，后续接 PDF/Word/模板渲染时直接复用同一出口
- 再往前推一步：新增 `TestReportDocument` / `TestReportDocumentMapper`，先把 `TestRecordAggregate` 映射为独立报告文档模型，避免渲染器直接绑定领域聚合，为后续 Word/PDF/模板渲染留出稳定边界
- 本小时继续收口报告边界：在 `TestReportDocument` 中补 `Metadata` 与 `AccompanyProduct`，让后续正式报告导出不必再直接反查领域聚合
- 修正 `TestReportDocumentMapper` 中的产品标识命名漂移：从错误的 `ProductDefinitionId` 取值切回领域对象真实字段 `ProductId`
- 补充控制台摘要输出：记录聚合落库后直接打印 item codes / persisted 摘要，便于后续接真实消息总线或仓储时快速观察闭环状态
- 尝试执行 `dotnet build/run`，但当前主机缺少 `dotnet` 命令，暂时只能完成静态校验，待环境具备后再做编译验证
- 继续把 Test 文档待办往前推：新增 `ITestReportRepository` 与 `InMemoryTestReportRepository`，把“报告文档 + 渲染结果”的持久化边界单独抽出，不再把报告导出仅停留在内存字符串
- 调整 `TestReportExportService`：拆成 `BuildDocument(record)` 与 `Export(document, renderer)` 两步，明确“记录聚合 -> 报告文档 -> 渲染输出”三段边界
- 更新 `TestBootstrap`：在记录聚合与附件样板保存之后，继续执行报告文档构建、JSON 渲染以及报告仓储保存，形成“聚合/附件/报告”三个持久化落点的最小闭环
- 本小时继续把报告出口从“仅内存字符串”往前推：新增 `ITestReportArtifactWriter` + `FileSystemTestReportArtifactWriter`，让 JSON 报告预览可以直接落盘为 artifacts/reports 下的真实文件，便于后续替换为 Word/PDF/模板导出链路
- 继续收口报告制品边界：新增 `TestReportArtifactDescriptor`，把输出文件名、格式、保存路径、写出时间统一建模；同时调整 `ITestReportArtifactWriter` / `PersistedReportArtifact` / `TestBootstrap`，避免后续接 Word/PDF 时还在调用方散落拼文件名与路径逻辑
- 本小时再往前推一步：新增 `TestReportPersistenceSummary`，并为 `ITestReportRepository` / `InMemoryTestReportRepository` 补 `SaveSummaryAsync`，把报告制品元信息（导出时间、文件名、保存路径、内容长度）从完整报告正文里再分离出一层轻量持久化摘要，便于后续接列表页、审计记录、导出历史查询
- 继续根据 Test 文档待办前推“仓储接口/映射策略”落地：为 `ITestRecordRepository` 补 `FindByRecordCodeAsync / ListRecentAsync`，并新增 `ITestRecordQueryService`、`TestRecordQueryService`，把记录查询/回放从单纯保存接口里独立出来
- 更新 `TestBootstrap`，在 demo 闭环里补一轮 recent list 与按 `RecordCode` 回读，先验证“记录生成 -> 保存 -> 查询摘要/回放”的最小链路
- 本小时继续把 Test 文档待办里的“SQLite/EF or Dapper 路线择一”往前推：先补 `SQLiteTestPersistence` 与一组 `SQLite*Repository` 样板，当前直接把产品定义、记录聚合 JSON、记录/分项附件、报告正文、报告摘要落到统一 SQLite 文件，先建立真实持久化落点，再决定后续是继续细化 SQLite 方案还是切 EF/Dapper
- 同步更新 `StandardTestNext.Test.csproj` 引入 `Microsoft.Data.Sqlite`，并在 `StandardTestNext.Test/README.md` 补充 SQLite 持久化边界说明，避免这条新落点只存在于代码里
- 继续把“报告输出抽象接口”往可查询方向推进：新增 `ITestReportQueryService` + `TestReportQueryService`，把报告历史摘要查询从仓储保存动作里拆出来
- 为 `ITestReportRepository`、`InMemoryTestReportRepository`、`SQLiteTestReportRepository` 补 `ListRecentSummariesAsync`，让导出历史列表/审计页有最小可复用查询入口
- 更新 `TestBootstrap`，在 demo 闭环里补一轮 recent report summaries 回读，验证“记录生成 -> 报告导出 -> 摘要保存 -> 摘要查询”不是纸面接口
- 本小时补做文档收口：将 `StandardTestNext.Test/README.md` 明确更新为当前真实闭环状态，补充 recent records / recent report summaries / record reload 已进入启动输出，避免维护文档与 repo 内说明继续脱节
- 同步确认这轮并未再凭空扩散新代码面，主价值是把已落地的查询/报告闭环状态整理清楚，为下一步把 SQLite 真实接入 `TestBootstrap` 做准备
- 按 Test 文档待办继续前推：已将 `SQLiteProductDefinitionRepository` / `SQLiteTestRecordRepository` / `SQLiteRecordAttachmentRepository` / `SQLiteTestReportRepository` 真正接入 `TestBootstrap`
- `TestBootstrap` 新增 `STNEXT_TEST_PERSISTENCE` 模式选择，当前支持 `memory`（默认）与 `sqlite`
- 当切到 `sqlite` 时，启动链路会自动初始化 `artifacts/test-persistence/standardtest-next.db`，并复用同一套 recent records / report summaries / record reload 验证闭环，而不再只是 SQLite 样板代码静态存在
- 本小时继续根据 Test 文档待办推进“样本映射策略独立出来”：新增 `TestRecordItemMappingResult` / `TestRecordSamplePartitionSummary` / `TestRecordStatistics` / `TestRecordBuildResult`
- 继续把 Test 文档待办里的“开始补报告输出抽象接口”往前推：新增 `MarkdownTestReportRenderer`，让 `ITestReportRenderer` 在现有 JSON 预览之外，先具备可落盘、可人工查阅的 Markdown 报告草稿输出能力，后续接正式模板渲染时可直接复用同一导出边界
- `TestRecordAggregateBuilder` 现在返回构建结果对象，不再只裸返回聚合；这样 `Bootstrap`、控制台摘要、报告导出都能复用同一份统计与映射信息
- `TestReportDocument` 新增 `Statistics` 区块，`TestReportDocumentMapper` / `TestReportExportService` / `TestRecordConsolePresenter` 已接入这层摘要，当前 JSON 预览和控制台输出都能直接看到 key point / continuous 的样本数量
- 本小时继续把“记录查询边界”从仅回读聚合推进到可回读附件：为 `IRecordAttachmentRepository` 增加 `ListForRecordAsync / ListForRecordItemAsync`，并同步补齐 `InMemoryRecordAttachmentRepository` 与 `SQLiteRecordAttachmentRepository` 的查询实现
- 新增 `TestRecordDetail`，让 `ITestRecordQueryService` / `TestRecordQueryService` 返回“记录聚合 + 记录级附件 + 分项附件桶”的组合结果，而不是只把 `TestRecordAggregate` 裸返回给调用方
- 更新 `TestBootstrap` 的 demo 回读输出：按 `RecordCode` 重载记录后，额外打印 record attachments 与 item attachment buckets 数量，证明当前闭环已经从“可写入”推进到“可查询回读”
- 本小时继续把 Test 文档待办里的“报告摘要/导出制品引用并入查询对象”落成真实代码：新增 `TestReportSnapshot`，为 `ITestReportRepository` / `ITestReportQueryService` 补 `ListForRecordCodeAsync`，并让 `TestRecordQueryService` 在 `TestRecordDetail` 中同时回填 report snapshots / report summaries
- 同步更新 `TestBootstrap` 回读输出：除 recent report summaries 外，额外打印 record reports 与 reloaded reports 数量，验证“记录 -> 报告正文/摘要 -> 详情查询”已经接成同一条最小闭环
- 继续按文档待办前推启动配置边界：新增 `TestStartupOptions` / `TestStartupOptionsParser`，`TestBootstrap` 不再只靠 `STNEXT_TEST_PERSISTENCE` 环境变量切换模式，现已支持 `--persistence memory|sqlite` 与 `--sqlite-db <path>`，为后续接正式配置文件与部署脚本先铺平入口
- 补做 repo 内文档对齐：确认 `StandardTestNext.Test/Program.cs` 已真实接通 `TestStartupOptionsParser.Parse(args)`，避免 README / 迁移计划写了“支持命令行参数”但入口文件仍停留在旧状态
- 同步更新 `StandardTestNext.Test/README.md`、`docs/MIGRATION_PLAN.md`、`docs/VERTICAL_SLICE_01.md`，把“命令行参数已接通、下一步转向统一配置入口”写成当前事实，减少维护文档与代码状态漂移
- 尝试读取腾讯文档 App/Test 维护文档以执行本小时回写，但当前 `tencent-docs get_content` 被 MCP access limit 拦截（`-32603`），本小时先整理好待回写内容，待额度恢复后再补同步到在线文档
- 本小时继续按待办把记录详情 DTO 往正式查询模型收口：新增 `TestRecordItemDetail`，把 `ItemCode / MethodCode / RecordMode / SampleCount / AttachmentCount / IsValid / Remark` 显式建模
- `TestRecordDetail` 已新增 `ItemDetails`，`TestRecordQueryService` 会从 `DataJson` 尝试解析 `SampleCount` 与 `RecordMode`，减少后续详情页/API 对原始 JSON 结构的直接依赖
- `TestBootstrap` 新增 `Reloaded item details` 输出，便于后续把 item 级统计摘要平移到 API/UI 查询模型
- 本小时继续把导出制品引用（artifact file/path）并入记录详情：`TestReportSnapshot` 已新增 `ArtifactFileName / ArtifactSavedPath`，`InMemoryTestReportRepository` 与 `SQLiteTestReportRepository` 查询时会直接回填报告制品元信息
- 同步更新 `TestBootstrap` 输出，当前 `Record reports` 已直接打印 `RecordCode:Format:ArtifactFileName`，便于后续把控制台验证平移到 API/UI 查询模型
- 当前下一步重点：继续把记录详情中的报告正文/摘要/制品信息收敛为更稳定的 API/DTO 视图，并开始收口正式报告模板边界
- 本小时继续按 Test 文档待办前推“统一配置入口”：新增 `TestRuntimeConfiguration` / `TestRuntimeConfigurationLoader` 与 `appsettings.test.json`，让 `TestBootstrap` 默认先读配置文件，再由环境变量、命令行覆盖，避免运行参数继续只挂在 CLI/ENV 上
- `TestStartupOptionsParser` 已支持 `--config <path>` / `--config=<path>`，当前可显式加载部署目录下的运行配置文件，为后续多环境发布与脚本化启动先铺平入口
- 同步更新 `StandardTestNext.Test.csproj` 复制 `appsettings.test.json` 到输出目录，并对齐 `README.md` / `docs/MIGRATION_PLAN.md` / `docs/VERTICAL_SLICE_01.md`，避免 repo 文档再次落后于代码状态
- 腾讯文档 App/Test 维护文档本轮仍因 `tencent-docs get_content` access limit（`-32603`）未能在线回写；已先在 repo 内整理真实变更，待额度恢复后优先补同步
- 本小时继续把记录详情 DTO 往稳定查询视图收口：`TestRecordItemDetail` 新增 `HasRemark`，`TestRecordDetail` 新增 `HasReports / HasReportArtifacts` 派生属性，`TestBootstrap` 回读输出同步打印 remark/artifact 状态，减少后续详情页/API 再次扫描原始字段或报告快照集合
- 本小时继续按 App/Test 维护文档待办推进“消息总线可插拔化”：新增 `IMessageBus`、`MessageBusOptions`、`MessageBusFactory`，将 App/Test 的 Program 与 Bootstrap 从具体 `InMemoryMessageBus` 类型签名改为依赖总线抽象；当前先保留 `inmemory` provider，并在双端配置文件中补齐同构的 `messageBus.*` 连接参数，作为后续接 MQTT 的最小配置位
- 本小时继续把“配置校验/启动前自检”从文档待办推进到真实代码：新增 `RuntimeConfigurationValidator` 与 `RuntimeConfigurationConsoleReporter`，双端入口现会在启动前打印 runtime/messageBus 摘要，并对 provider、port、clientId、topicPrefix、samplingMode、persistenceMode、sqliteDbPath 给出最小告警
- 同步修正 `StandardTestNext.App/Program.cs` 与 `StandardTestNext.Test/Program.cs` 的串线问题：两个可执行入口各自只启动自身 Bootstrap 与配置，不再互相拉起对方运行链路；后续若需联调宿主，应单独建 integration/demo host，而不是继续污染单端入口
- 本小时继续按在线维护文档待办前推一项可直接落地的小改动：`StandardTestNext.App/ContractsBridge/IMessageBus.cs` 已不再继承 obsolete 的 `IMessageSubscriber` shim，而是直接声明 `Subscribe<T>`，避免当前运行主干继续背着已废弃兼容层
- 复验结果：`dotnet build StandardTestNext.sln --no-restore` 当前 0 warning / 0 error；`dotnet run` App/Test 两个入口均可启动，App 侧可输出设备状态/实时样本，Test 侧可完成命令发布、记录聚合、报告落盘与回读摘要
- 本轮仍尝试读取腾讯文档 App/Test 维护文档，但继续被 `tencent-docs` MCP `access limit (-32603)` 阻断；已将待同步内容先落到 `tmp_app_doc.md` / `tmp_test_doc.md`，待额度恢复后优先回写
- 本小时继续把 Test 侧“产品定义仓储接口”从只读写样板推进到可复用应用服务：新增 `ITestProductDefinitionService` / `TestProductDefinitionService`，按 `productKind` 复用已有产品定义，并在额定参数快照变化时原位更新 `Model / Code / RatedParamsJson`
- `TestRecordAggregateBuilder` 不再强制内部 new `ProductDefinition`，改为接收外部解析好的 `productDefinition`；`TestBootstrap` 现已先走产品定义解析，再生成记录聚合，后续切真实任务/多次试验时可稳定复用产品主数据
- 本小时继续把 Test 文档待办里的“更稳定的 API/DTO 视图”往前推：`TestRecordSummary` 已新增 `ReportCount / HasReportArtifacts / LatestReportSavedAt / ProductCode / ProductModel / ReusedProductDefinition`，`TestRecordQueryService.ListRecentAsync` 会直接拼出最近记录的报告存在性、制品状态与产品定义复用状态，`TestBootstrap` 输出也同步改成带产品/报告状态的 recent records 摘要，减少后续列表页/API 再逐条扫报告仓储或产品主数据
- 本小时继续沿着同一待办补产品主数据查询边界：新增 `IProductDefinitionQueryService` / `ProductDefinitionQueryService`，并为 `IProductDefinitionRepository`、`InMemoryProductDefinitionRepository`、`SQLiteProductDefinitionRepository` 补 `ListRecentAsync`，让后续产品定义列表、记录详情主数据回查、主数据对账不必再把查询逻辑塞回 Bootstrap
- 本轮继续把这条查询边界真正接进 demo 启动链路：`TestBootstrap` 已补 recent products 与 `GetByKind(productKind)` 回读输出，确保产品主数据查询不只是留在接口/仓储层，而是进入可观察的运行主干
- 本小时继续把“报告输出抽象接口”从存在推进到真正使用：`TestBootstrap` 已同时导出 JSON 与 Markdown 两种报告格式，并将两种格式都写入 `ITestReportRepository` 与报告摘要；当前 report history 不再只覆盖 JSON 预览，Markdown 草稿也进入同一条查询/持久化链路
- 本小时继续把记录查询 DTO 组装从 `TestRecordQueryService` 内部往外抽：新增 `TestRecordQueryViewAssembler`，统一装配 recent/detail 两条查询路径的 `ItemDetails / Mapping / ReportSummaries`，避免查询服务再次膨胀成展示拼装层
- 同步新增 `TestRecordItemPayloadReader`，把 `DataJson` 中 `SampleCount / RecordMode` 的解析逻辑收口到独立读取器，后续 API/报告层如果需要复用 item payload 摘要，不必重复写 JSON 解析
- `TestRecordDetail` 本轮新增 `Mapping` 聚合快照，`TestBootstrap` 回读输出也已补 `Reloaded mapping`，说明详情查询不再只回 item 列表，而是能直接给出 `samples/kp/cont` 级摘要
- 本轮已复验 `dotnet build StandardTestNext.sln --no-restore` 与 `dotnet run --project StandardTestNext.Test/StandardTestNext.Test.csproj --no-build`，继续保持可编译可运行状态后再更新维护文档
- 本小时继续把“报告输出抽象接口”往正式报告边界前推：新增 `TestReportManifest` / `TestReportManifestMapper` / `ManifestTestReportRenderer`，并让 `TestBootstrap` 在 JSON / Markdown 之外同步导出 `manifest.json` 概览制品，后续索引页、审计摘要、导出目录页可优先消费轻量 manifest，而不是直接依赖完整报告正文
- 本小时继续把轻量查询视图从“存在于 mapper 扩展方法”推进到“存在于正式应用服务契约”：`ITestRecordQueryService` 新增 `ListRecentViewsAsync / GetDetailViewByRecordCodeAsync`，`TestRecordQueryService` 与 `InMemoryTestRecordQueryService` 都直接返回 `TestRecordListView / TestRecordDetailView`，`TestBootstrap` 也改为消费同一条 view 查询出口，避免后续 App/API 再自行做 `ToListView()/ToDetailView()` 二次映射
- 本小时继续顺着 App/Test 查询契约待办往前推：`StandardTestNext.Contracts/TestRecordContracts.cs` 的 recent list 契约新增 `RecordAttachmentCount / ItemAttachmentBucketCount`，`TestRecordSummary / TestRecordListView / TestRecordQueryViewAssembler / TestRecordViewMapper` 已同步把记录级附件数、分项附件桶数、primary artifact、lightweight artifact 一并收敛到 recent list 视图里
- 同步更新 `TestRecordQueryGatewayAdapter`、`TestRecordQueryGatewayStub`、`AppBootstrap` 与 `TestBootstrap` 的消费路径；当前不只是 detail，连 App 侧 recent list 预览也能直接看到 `attachments + primary/lightweight report artifact` 摘要，为下一步用真实查询网关替换 stub 先补齐稳定列表合同

## 2026-03-22 02:11 采样分区元数据收口
- 本小时继续按 Test 维护待办把“样本映射策略”往可复用查询/报告边界推进：为 `TestRecordSamplePartitionDescriptor` 增加 `DisplayName` 与 `SortOrder`，不再只留下 `ItemCode/RecordMode/MethodCode` 这类偏机器语义字段。
- `TestRecordSamplePartitioner` 现已按 `SortOrder -> ItemCode` 输出稳定顺序；当前默认把 `RealtimeKeyPoints` 固定排在 `RealtimeContinuous` 前面，避免报告、详情视图、后续 App 侧列表继续依赖插入顺序或 `if/else` 猜测展示顺序。
- `TestRecordItemMappingResult.TestRecordSamplePartitionSummary`、`TestRecordItemDetail`、`TestRecordMappingSnapshotFactory` 已同步携带这层元数据，说明“关键点记录/连续采样记录”的显示语义开始从 builder 内部硬编码抽离出来，而不再只存在于控制台输出或注释里。
- `TestRecordQueryViewAssembler` 已把 detail item 明细改为稳定排序，并为 `RealtimeKeyPoints / RealtimeContinuous` 回填显式 `DisplayName`，这样后续 App/API/报告模板可以先消费这层稳定语义，减少再次散落 `ItemCode -> 展示名` 的映射逻辑。
- 本轮没有新建 git 提交；改动仍处于 `next-gen/` 工作区未提交状态。

## 2026-03-22 02:41 App/Test 查询契约补齐
- 本小时先复核腾讯文档 App/Test 维护文档与 `next-gen/` 当前工作区，确认上一轮已把 Test 侧 `ItemDetails / ReportSummaries` 扩进共享 contracts，但 App 侧 `TestRecordQueryGatewayStub` 仍停留在旧 detail 占位结构。
- 已更新 `StandardTestNext.App/Application/TestRecordQueryGatewayStub.cs`：现在 stub detail 会显式返回 `PrimaryReportFormat / LightweightReportFormat`、两条 `ItemDetails`（关键点 / 连续采样）、以及 JSON + manifest 两条 `ReportSummaries`，让 App 侧在真实网关尚未接通前，也能按最新共享契约演练记录详情展示。
- 这一步的价值不是美化 demo，而是避免 `StandardTestNext.Contracts/TestRecordContracts.cs`、`StandardTestNext.Test/Application/AppSide/TestRecordQueryGatewayAdapter.cs` 与 App 侧 stub 三处再次发生契约漂移；当前“Contracts → Test adapter → App stub”三点的 detail 结构已重新对齐。
- 本轮未新建 git 提交；改动继续保留在 `next-gen/` 工作区，待后续把真实 App 查询消费入口接上后再与同批重构一起提交。
- 本小时继续按待办做一项可以直接收口的清理：删除 `StandardTestNext.App/ContractsBridge/TestRecordDetailContract.cs` 与 `StandardTestNext.App/ContractsBridge/TestRecordListItemContract.cs` 两个 App 侧历史合同镜像壳文件。它们只是在本地再包一层 `StandardTestNext.Contracts` alias，继续保留会弱化“共享 contracts 已集中到 Contracts 项目”的边界表达。
- 删除后已立即复验 `dotnet build StandardTestNext.sln --no-restore`，结果继续为 0 warning / 0 error；说明当前 App 侧已可直接依赖共享 contracts，而不需要额外的本地镜像文件。

## 2026-03-22 00:11 runtime bridge 收口尝试与回退
- 本小时尝试把 `StandardTestNext.Test/Application/RuntimeBridge` 下的 `IMessageBus / IMessageBusConfiguration / IMessagePublisher / IMessageSubscriber` 改成对 `StandardTestNext.Contracts` 的 alias，目标是减少 Test 侧重复接口定义。
- 随后立即执行 `dotnet build StandardTestNext.sln --no-restore` 验证，实际触发 `CS8914`：`global using directive cannot be used in a namespace declaration`，并连带造成 `IMessageBus`、`IMessageBusConfiguration` 等类型在 Test 工程内无法解析。
- 因此本轮已把 4 个文件全部回退到原始接口定义，保持当前主干可继续编译；这次推进的真实价值不是留下了新结构，而是确认了一条错误迁移路径，避免后续再在同一坑里浪费时间。
- 下一步应改为：要么直接让 RuntimeBridge 实现类引用 Contracts 命名空间；要么通过批量替换类型引用/删除本地重复接口来完成统一，不能继续走“namespace 内 global using alias”这条路。

## 2026-03-22 07:41 App 查询默认入口收口
- 本小时继续沿着“不要再扩 stub 字段，而是开始收口真实消费入口”的待办往前推：`StandardTestNext.App/Application/AppBootstrap.cs` 不再直接 new `TestRecordQueryGatewayStub`，改为统一走 `TestRecordQueryGatewayDefaults.Create()`。
- 这一步看起来很小，但它把 App 侧“默认查询网关从哪里来”正式收成了一个单点；后续无论切成本地 stub、进程内 adapter、还是跨进程/HTTP/MQTT 查询网关，都不用再回头改 `AppBootstrap` 主流程。
- 同步补齐 `StandardTestNext.App/Application/TestRecordQueryGatewayStub.cs` 的默认工厂类，明确当前默认实现仍是 stub，但切换点已经从调用点内联构造，收敛成可替换入口。
- 本轮没有新建 git 提交；改动仍在 `next-gen/` 工作区未提交状态。

## 2026-03-22 16:41 App 默认查询入口去掉编译期 Test 依赖并收口 seeded contract
- 本小时继续沿着“让 App 默认查询走真实 seeded 数据，但不要把 App 主项目重新绑死到 Test 工程”这条线收口：新增/落定 `StandardTestNext.App/Application/InProcAppQueryGatewayFactory.cs`，改为通过反射装配 `StandardTestNext.Test.Application.AppSide.InProcAppQueryGatewaySeedFactory`，而不是在 `Program.cs` 里直接引用 Test 侧类型。
- 同步删除 `StandardTestNext.App/StandardTestNext.App.csproj` 中对 `StandardTestNext.Test` 的直接 `ProjectReference`；当前 App 编译期只显式依赖 `StandardTestNext.Contracts`，但默认 recent/detail/report summaries 预览依旧能吃到 seeded in-proc 的真实记录/报告数据。
- 为避免演示样本再次散落在多个入口，本小时新增 `StandardTestNext.Contracts/TestRecordQuerySeedContracts.cs`，把默认 rated params + realtime samples 收成共享 `TestRecordQuerySeedFactory.CreateDefault()`；后续无论 smoke host、App 默认查询宿主还是别的 demo 入口，都可以复用同一套最小种子。
- `TestRecordQueryViewAssembler` 同步改为经 `TestReportSelection.SelectPrimary/SelectLightweight` 选择主报告与轻量报告，不再依赖仓储返回顺序；lightweight fallback 当前优先 `manifest`，最后才退到最早保存的一条报告。
- 已复验 `dotnet build StandardTestNext.sln --no-restore`，结果继续为 `0 warning / 0 error`。
- 这一步的真实价值不是“又多一个工厂”，而是把下一步切换点收得更清楚：后续若演进为 `remote / sqlite-backed / configurable null-fallback`，优先替换这层入口与装配策略，不必再回头拆 App 主流程。

## 2026-03-22 11:41 App 默认查询从空仓储 adapter 推进到 seeded in-proc gateway
- 本小时继续按 App/Test 维护待办推进“不要再让 App 默认查询链路吃假/空数据”，新增 `StandardTestNext.Test/Application/AppSide/InProcAppQueryGatewaySeedFactory.cs`，把 App 默认查询网关收敛成一个可复用的 seeded in-proc gateway 入口。
- 这次不是简单 `new adapter + 空 in-memory 仓储`：factory 会先构造 demo rated params、生成一份最小 `TestRecordAggregate`、再补三种报告摘要（json / md / manifest），随后才暴露 `TestRecordQueryGatewayAdapter` 给 App 消费。这样 App 现在默认至少能通过真实 query facade/query service 走通一次“recent list + detail + report summaries”链路，而不是 adapter 外壳真实、仓储内容却始终为空。
- `StandardTestNext.App/Program.cs` 已改为直接调用 `InProcAppQueryGatewaySeedFactory.CreateSeededGateway`；这也把下一步切换策略收得更清楚：后续若切 `remote / sqlite-backed / null-fallback`，只需要替换工厂解析，不必再回头拆 `Program` 或 `AppBootstrap`。
- 当前仍未新建 git 提交；改动继续保留在 `next-gen/` 工作区未提交状态。

## 2026-03-22 09:11 报告选择逻辑收口
- 本小时继续按 Test 维护文档中的“收口查询/报告边界”推进，新增 `StandardTestNext.Test/Application/Services/TestReportSelection.cs`，把 recent list / detail 视图依赖的 primary report 与 lightweight report 选择逻辑收成单点，而不再散落在查询组装器里直接 `FirstOrDefault`。
- `TestRecordQueryViewAssembler.AssembleSummary(...)` 与 `AssembleDetail(...)` 现已统一调用 `TestReportSelection.SelectPrimary/SelectLightweight`；这样后续即使报告仓储返回顺序变化，或者未来增加更多报告格式，列表侧和详情侧都能稳定挑出主报告与轻量报告。
- 这一步虽然小，但它是在把“报告主次判定”从偶然的集合顺序，往显式规则收口；后续 App 查询网关、列表 DTO、详情 DTO、导出历史页都可以复用这层规则，而不用各自再猜一次。
- 本轮没有新建 git 提交；改动仍在 `next-gen/` 工作区未提交状态。

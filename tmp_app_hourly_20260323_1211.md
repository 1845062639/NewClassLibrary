App维护文档

本次维护说明
维护时间：2026-03-23 12:15 Asia/Shanghai
本小时原计划先直接更新腾讯文档 App/Test 维护页，但在读取两份远端文档时腾讯文档 MCP 返回 400007 access limit，当前这一步未能成功写回远端，因此先基于 next-gen/ 当前工作区与本地维护草稿继续推进真实开发，避免本小时只卡在文档接口上空转。开发推进仍聚焦 next-gen/ 主干，而不是继续扩散旧 app-repos/StandardTestApp 历史项目改动。

本小时 App 侧新增的真实推进点：
1. 把 App 查询网关模式（auto / seeded-inproc / null-fallback）的解析与默认回退补成可执行 smoke tests，不再只靠人工读控制台。
2. 给 App 入口增加 --smoke / --run-smoke-tests 轻量校验模式，便于后续每小时维护时快速验证 query gateway 配置链路。
3. 在尝试把这类校验塞进 Test 项目时，真实发现了“Test 不应反向依赖 App”的边界问题，随后改为把 smoke harness 留在 App 项目内部，避免引入反向项目引用污染。

代码修改记录
本小时 App 侧直接新增/变更的真实代码：
- next-gen/StandardTestNext.App/Application/Services/AppQueryGatewaySmokeTests.cs
  - 新增 App query gateway smoke tests。
  - 覆盖环境变量 STNEXT_APP_QUERY_GATEWAY 对 QueryGatewayMode 的覆盖解析。
  - 覆盖 CLI 参数 --query-gateway=seeded-inproc / --query-gateway none 的别名解析。
  - 覆盖 InProcAppQueryGatewayFactory.ResolveDefaultGateway 在 NullFallback / SeededInProc / Auto 下的最小回归验证。
- next-gen/StandardTestNext.App/Program.cs
  - 新增 --smoke / --run-smoke-tests 入口。
  - smoke 模式下先执行 AppQueryGatewaySmokeTests.Run()，通过后直接退出，避免每次都进入完整 App 运行链路。

本小时同步复核、仍与 App 主线直接相关但不是本小时新增的工作区文件：
- next-gen/StandardTestNext.App/Application/Services/AppQueryGatewayMode.cs
- next-gen/StandardTestNext.App/Application/Services/AppStartupOptions.cs
- next-gen/StandardTestNext.App/Application/Services/AppStartupOptionsParser.cs
- next-gen/StandardTestNext.App/Application/InProcAppQueryGatewayFactory.cs
- next-gen/StandardTestNext.Contracts/TestRecordLegacyPayloadFormatter.cs
这些文件继续构成当前 App 查询网关与 legacy payload 摘要收口的主干。

Git 提交记录
本小时没有新增 git 提交。
当前 next-gen/ 最近相关提交仍为：
- f3e83b7 feat: expose legacy payload summaries in query contracts
- cd7973d refactor: add null fallback for app query gateway
- 21bf4fe refactor: align test runtime bridge with shared contracts
- a954cac refactor: unify test record contracts and partition pipeline
- a9b8448 refactor: decouple app seed gateway wiring

本小时真实验证
- 读取腾讯文档 App/Test 维护文档：失败，腾讯文档 MCP 返回 400007 access limit。
- dotnet build next-gen/StandardTestNext.sln --no-restore -nologo ✅
- dotnet run --project next-gen/StandardTestNext.App/StandardTestNext.App.csproj --no-build -- --smoke ✅
- dotnet run --project next-gen/StandardTestNext.Test/StandardTestNext.Test.csproj --no-build -- --smoke ✅
当前结果：0 Warning / 0 Error。

待办事项
- 腾讯文档额度恢复后，优先把本小时 App/Test 维护内容真实写回 file_id=DWWN4S01oY21JaFVI 与 file_id=DWUNpRERyY01pcVhK，补齐远端维护说明、代码修改记录、Git 提交记录、待办事项、下次更新时间。
- 继续把 App 侧 query gateway 的 smoke harness 从模式解析/回退，向更贴近真实查询消费的最小回归验证推进。
- 继续把 App 默认查询入口从 seeded in-proc / null fallback，推进到更稳定的可配置策略边界。
- legacy payload 摘要链路稳定后，继续把输出从 bootstrap/控制台打印收口到更明确的 presenter / application service 边界。

下次更新时间
2026-03-23 13:11（Asia/Shanghai）

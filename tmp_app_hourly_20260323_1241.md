# App维护文档

## 本次维护说明
- 维护时间：2026-03-23 12:41 Asia/Shanghai
- 本小时先检查了 StandardTestApp / next-gen 两侧工作区与最近提交，再尝试读取并更新腾讯文档 App/Test 维护文档；但腾讯文档 MCP 在读取阶段即返回 **400007 access limit**，因此本轮无法把更新直接写回远端文档。
- 在远端文档受限的情况下，本小时仍继续按待办直接推进 next-gen 的 App/Test 边界重构，不空转等待：重点把 **App 查询网关入口** 从隐式默认行为推进为 **显式模式 + 显式 smoke 入口 + 启动可观测输出**。
- 这轮还顺手收口了一个实际运行层面的细节：此前 Test 进程正常启动时会直接执行 smoke 测试，容易把“正常运行入口”和“验证入口”混在一起。本小时已改成仅在显式传入 `--smoke` / `--run-smoke-tests` 时才执行 smoke。
- 已完成真实验证：
  - `dotnet build StandardTestNext.sln --no-restore -nologo` ✅
  - `dotnet run --project StandardTestNext.App/StandardTestNext.App.csproj --no-build -nologo -- --smoke` ✅
  - `dotnet run --project StandardTestNext.Test/StandardTestNext.Test.csproj --no-build -nologo -- --smoke` ✅
- 当前结果：App/Test 两侧 smoke 入口都能独立运行；正常 Test 启动不再默认执行 smoke；App 查询网关模式支持显式 requested/resolved 观测。

## 代码修改记录
- `next-gen/StandardTestNext.App/Application/Services/AppQueryGatewayMode.cs`
  - 新增 App 查询网关模式枚举：`Auto / SeededInProc / NullFallback`。
- `next-gen/StandardTestNext.App/Application/Services/AppStartupOptions.cs`
  - 新增 `QueryGatewayMode` 启动选项。
- `next-gen/StandardTestNext.App/Application/Services/AppStartupOptionsParser.cs`
  - 支持解析环境变量 `STNEXT_APP_QUERY_GATEWAY`。
  - 支持解析命令行 `--query-gateway` / `--query-gateway=`。
  - 兼容值：`auto`、`seeded-inproc|seeded|inproc`、`null-fallback|null|none`。
- `next-gen/StandardTestNext.App/Application/InProcAppQueryGatewayFactory.cs`
  - 默认查询网关解析改为接受 `AppQueryGatewayMode`。
  - `Auto` 优先 seeded，失败再 null fallback；`SeededInProc` 与 `NullFallback` 可显式指定。
  - 去掉字符串反射，直接调用 `StandardTestNext.Test.Application.AppSide.InProcAppQueryGatewaySeedFactory.CreateSeededGateway()`。
- `next-gen/StandardTestNext.App/Program.cs`
  - 新增 `--smoke` / `--run-smoke-tests` 显式 smoke 入口。
  - 启动时打印 `queryGateway.requested` 与 `queryGateway.resolved`。
- `next-gen/StandardTestNext.App/Application/AppBootstrap.cs`
  - 新增 list/detail 两侧 legacy payload summary 控制台输出。
- `next-gen/StandardTestNext.App/StandardTestNext.App.csproj`
  - 新增对 `StandardTestNext.Test` 的项目引用，保证 seeded gateway 稳定可用。
- `next-gen/StandardTestNext.Contracts/TestRecordLegacyPayloadFormatter.cs`
  - 继续作为 App/Test 共用的 legacy payload summary 格式化能力。
- `next-gen/StandardTestNext.Test/Application/Services/TestRecordLegacyPayloadReaderSmokeTests.cs`
  - 补充 legacy payload 解析/摘要格式化 smoke 覆盖。
- `next-gen/StandardTestNext.Test/Application/Services/TestRecordQueryGatewayAdapterSmokeTests.cs`
  - 补充 query gateway adapter 对 legacy payload summary 暴露的 smoke 验证。
- `next-gen/StandardTestNext.Test/Application/Services/TestStartupOptions.cs`
  - 新增 `RunSmokeTests` 选项。
- `next-gen/StandardTestNext.Test/Application/Services/TestStartupOptionsParser.cs`
  - 支持 `--smoke` / `--run-smoke-tests` / `--no-smoke`。
- `next-gen/StandardTestNext.Test/Program.cs`
  - 改成仅在显式 smoke 参数下执行 smoke 测试。
- `next-gen/StandardTestNext.Test/Application/TestBootstrap.cs`
  - 移除正常启动流程中的默认 smoke 执行，避免污染主运行路径。
- `next-gen/StandardTestNext.Test/Application/Services/TestRecordConsolePresenter.cs`
  - 输出 legacy payload summary，便于 Test 侧联调观察。

## Git 提交记录
- 本小时已新增 git 提交：
  - `2de2c7c refactor: add explicit app query gateway smoke entrypoints`
- 当前 next-gen 最近提交：
  - `2de2c7c refactor: add explicit app query gateway smoke entrypoints`
  - `f3e83b7 feat: expose legacy payload summaries in query contracts`
  - `cd7973d refactor: add null fallback for app query gateway`

## 待办事项
1. 在腾讯文档额度恢复后，把本地这轮维护记录同步回 App 维护文档 `DWWN4S01oY21JaFVI`。
2. 继续把 `seeded-inproc` 查询来源推进到更真实的持久化/远程来源（优先 sqlite-backed query gateway）。
3. 给 App 查询侧再补一轮最小回归，覆盖 requested/resolved 与 seeded/null 两条路径的运行输出一致性。
4. 评估当前新增的 App->Test 项目引用是否需要在后续用更轻的 contracts/adapter 分层替代，避免长期反向耦合。

## 下次更新时间
- 预计下次更新时间：2026-03-23 13:41（Asia/Shanghai）
